services:
  app:
    image: ghcr.io/onkr0d/titanic/trimmer:v1.2.0@sha256:4b5ecc5ddd89139dfd0338fb02f37b3082d52f46b660ccbd5b6d4fbfb42e320f
    user: "0:0"
    restart: unless-stopped
    ports:
      - "${APP_TRIMMER_PORT:-3030}:3030"
    environment:
      - RUST_LOG=info
      - MEDIA_PATH=/downloads
      - DATA_DIR=/data
    volumes:
      - ${APP_DATA_DIR}/data:/data
      - ${UMBREL_ROOT}/home/Downloads:/downloads
    healthcheck:
      test: ["CMD", "wget", "--no-verbose", "--tries=1", "--spider", "http://localhost:3030/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 20s
